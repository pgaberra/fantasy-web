# Decision log

Choices about fantasy-web that the code shows but cannot explain, one line each. The format and
the rules live in the monorepo root `DECISIONS.md`; decisions that span repos go there instead.

---

[2026-09-10] fantasy-web: icons are drawn by `@ng-icons/lucide` behind `<app-icon>`, not hand-copied into a template — copying each new icon by hand is how the set drifted to six stroke widths; `lucide-angular` was rejected because its peer range stops at Angular 21, and the earlier claim (#591) that this ruled out a package was wrong, since `@ng-icons/lucide` supports Angular 22.
[2026-09-10] fantasy-web: the "From scratch" starting point uses `square-dashed`, not `pencil` or a new-file icon — the pencil already means "edit" (change a pick, rename a projection), and all three cards create a projection, so what sets this one apart is that it starts empty.
[2026-09-11] fantasy-web: the note under the AI projection preview names its data source and not its method — the old line described a three-season, age-adjusted blend, which left out most of what the model now does (fitted availability, league level, finishing table, depth charts) and would go stale with every model version, while the MoneyPuck credit is the part the terms actually require.
[2026-09-11] fantasy-web: "Import league" uses Lucide's `import` (an arrow into a tray), not `refresh` or `download` — `refresh` stays with what genuinely syncs again ("Re-sync or change league", the player list notice), so importing and re-syncing no longer look like the same action, and `download` reads as saving a file to the user's machine, when this pulls settings into the app.
[2026-09-11] fantasy-web: icon sizes are a type (`IconSize`: 14, 16, 20, 48 or an `em` length), not a documented scale — with only the prose, the app drifted to 12, 13, 15, 18 and 22 as well; buttons and menus went from 15 up to 16 rather than down to 14, because 14 is the size for an icon inside running text, and 48 joined the scale because both empty and error states already used it on purpose.
[2026-09-11] fantasy-web: the AI projection preset follows the BFF's `GET /api/v1/features` (`FeatureService.aiProjection`) and the `AI_PROJECTION_ENABLED` build arg is gone — a web switch next to the BFF's had to agree with it by hand, and prod-web had it on while prod-bff refused the model; keeping the build arg as an extra kill switch was rejected because the BFF's `AI_PROJECTION_ENABLED` already turns the feature off everywhere without a web rebuild, and no deployed web app set it.
[2026-09-11] fantasy-web: the preset-draft E2E waits for the "Start a new draft" section to know the sources loaded, not for the Start button — the button is replaced by "Unlock with Premium" whenever the picked preset is locked, which is correct product behaviour; discarding the shared account's preset draft each run (so the start path runs daily) was passed over for now, since it adds a destructive step to a shared account for coverage the happy path's projection draft mostly gives.
[2026-09-11] fantasy-web: a line whose skater/goalie type disagrees with the pool is restarted from the player's own pool line (`squaredWithPool`, where the editor loads a board and where the starting-point preview ranks one), not guarded with `?? 0` in the cell or squared in the BFF reconciler — the model reads a player's type from the NHL and the pool from ESPN, which lists goalie Arsenii Sergeev as a centre, so the two will disagree again; a zero-filled cell would draw a goalie's line under skater columns and rank it as a goalie, and the reconciler never looks at rows it did not add.
[2026-09-11] fantasy-web: staging is kept out of search by an `X-Robots-Tag: noindex` header that nginx sends whenever `APP_ENV` is not `production`, not by a staging robots.txt with `Disallow: /` — a crawler has to fetch a page to learn it must drop it, so a Disallow would leave whatever staging already has indexed; keying on "not production" rather than "is staging" means a new environment starts hidden, at the price that a mistyped prod value hides prod, which DEPLOYMENT.md now warns about.
[2026-09-11] fantasy-web: nginx caches only js and css for a year as immutable, and images, icons and fonts for a day — the build hashes every script and stylesheet name, but the files in public/ keep a fixed name, so a year's cache kept a replaced favicon from anyone who had visited; a day was picked over `no-cache` so the logos on every page do not cost a revalidation request each load.
[2026-09-11] fantasy-web: a page is kept out of search by the noindex the app sets on every route not marked `indexable`, not by robots.txt — a Disallow stops the crawl but leaves an already-indexed URL listed, as /forgot-password was; a default-deny robots.txt would also block the bundle Googlebot renders with and the `/s/` links X's preview bot must reach, and an nginx X-Robots-Tag per path was passed over as a third list of routes to keep in step, so crawl-rules.spec.ts holds sitemap.xml and robots.txt to the route data instead.
[2026-09-11] fantasy-web: Paddle's environment is read off the client token's prefix (`test_` sandbox, `live_` production), not set by a `PADDLE_ENVIRONMENT` build arg — the arg could only agree with the token or contradict it, and left unset it silently meant production; a token of any other shape is reported as a build fault rather than defaulted to either account.
[2026-09-11] fantasy-web: `/pay` opens Paddle's one-page checkout, not its default multi-page one — the BFF now attaches the buyer's verified email, which leaves a separate email step with nothing to ask; the overlay mode is stated too rather than inherited from Paddle's defaults.
[2026-09-11] fantasy-web: the welcome after checkout is only the thanks, with no "Start an AI projection" / "Pick a range on Who's Hot" buttons — removed at Alexander's call, which also dropped `link`/`linkLabel` from `PremiumPerk` since nothing else read them; hiding the buttons but keeping the fields was passed over as data nothing reads.
[2026-09-11] fantasy-web: the two plan cards on /premium share their rows through CSS subgrid (head, perks, plan state, action, small print), not bottom-pushed footers or a fixed note height — pushing the action down lined the cards' last lines up, so a two-line note under one action and a one-line note under the other left the actions a line apart, and a min-height would hold only for the copy it was measured against; every action on the cards is a `btn-lg`, the size the "Your current plan" box was already drawn to.
[2026-09-11] fantasy-web: while Paddle's price preview loads, /premium says "Loading price" inside a price row held at the amount's own line height, and the free card's figure stays empty until the same answer arrives — the old stand-in line was shorter than the price, so its swap pushed both cards down, and "Free" turning into "0 kr" a beat later was a second jump; hiding the whole head until the price arrived was passed over because Paddle can fail or be slow, and the line that says the price is confirmed at checkout stays as the fallback once it has.
[2026-09-11] fantasy-web: Who's hot takes each season's length and its opening season from the BFF's `/projection-model/splits/seasons`, sends "Last N" as `lastGames`, and remembers a season only once the visitor picks one (settings version 3), replacing `SEASON_SCHEDULE_GAMES = 82`, `DEFAULT_SEASON_START_YEAR = 2025` and from/to pairs counted back from the schedule's end — 2026-27 is 84 games, mid-season each team stands on its own game number, and version 2 saved the season on every visit so a summer visit would have held the page on 2025-26 all winter; a web-side table of season lengths was rejected as a fourth copy of projection-service's rule.
[2026-09-11] fantasy-web: refunds stay a section of the terms, now under their own `#refunds` heading with a "Refunds" footer link, because Paddle's live domain review wants the refund policy reachable from the navigation — a separate refund page again (undoing web#564) was rejected to keep one contract, and the terms page scrolls to its own fragment instead of switching on the router's anchor scrolling, which would also act on the profile's `#username`.
[2026-09-11] fantasy-web: the Who's Hot "Min. games" box shows the minimum the user chose and is capped by the season, not the range; only the table's filter is clamped to the range's length (`appliedMinGames`) — #526 showed the clamped figure in the box so it always matched the running filter, but a setting that changes by itself when the range moves read as the page overriding the user (Alexander's call); dropping the clamp altogether was passed over because a minimum above the range's length empties the leaderboard with nothing on screen saying why.
[2026-09-11] fantasy-web: DEPLOYMENT.md's build-arg table is held to the Dockerfile's ARGs by `check-deployment-args.sh` in both directions, and CLAUDE.md names no Angular or TypeScript version, pointing at `package.json` instead — the table had drifted to 9 of 15 args and the versions a major behind with nothing comparing them; generating the table from Dockerfile comments was passed over because the rows carry per-environment advice the comments should not.
[2026-09-11] fantasy-web: a failed token refresh ends the session only when `/auth/refresh` answers 401 or 403 (the BFF's answer for an invalid, expired or revoked token), and parallel 401s share one in-flight refresh in `AuthService.refresh()` — a status 0 or 5xx says nothing about the token, and ending the session for one deleted a refresh token good for 30 days; retrying the refresh longer on a blip was passed over as the retry interceptor's job, and the share lives in the service rather than the interceptor so any later caller of `refresh()` gets it too.
[2026-09-11] fantasy-web: the CSP's `connect-src` names one API, the build's own `API_URL` through the existing `__API_ORIGIN__` substitution, instead of listing production's and staging's — a build given the wrong environment's `API_URL` now fails visibly rather than using the other environment's data; a per-environment nginx.conf was passed over as a second file to keep in step, and the placeholder check now also fails CI on a placeholder nginx.conf's serve-stage sed does not replace.
[2026-09-11] fantasy-web: the refund policy is a page of its own again (`/refunds`, linked from the footer) and the terms point to it instead of carrying refund wording — this reverses web#564 and supersedes the `#refunds` section line above, because Paddle's live account checklist asks for publicly accessible Terms, Privacy and Refund Policy pages; at Alexander's request the policy follows common SaaS practice (a first payment refunded in full within 14 days, started renewals not refunded, payments after cancelling, duplicates and unauthorised payments always refunded), and keeping a copy in the terms as well was rejected so the wording has one home.
[2026-09-13] fantasy-web: every request attempt is bounded by `timeoutInterceptor` (20 s for a read, 60 s for a write) and a timeout is not retried — a deadlocked BFF accepted connections and never answered, so pages spun for minutes; 20 s sits above the BFF's longest downstream wait (15 s) so a slow answer still lands, a bound of a few seconds was rejected for cutting those off, and retrying a timeout for tripling the wait on a server that already said nothing. A failure on our side (5xx, timeout, status 0 while online) now says "We're having technical problems" in the error state instead of blaming the connection; adding "we're working on it" was left out, first on the belief that nothing alerts anyone to an outage. That was wrong: the health monitor raised the 2026-09-11 outage within minutes, it just notified no one. Since 2026-09-13 it emails Alexander (Sentry rule 821976, see the workspace `DECISIONS.md`), so whether to add the line is his call.
[2026-09-14] fantasy-web: Premium can be shown before it is sold, through a `PREMIUM_COMING_SOON` build arg that disables Subscribe with a note and closes `/pay`, while `PAYMENTS_ENABLED` keeps the page, prices and badges — production opens to the public before Paddle has approved the domain, and Alexander chose to keep Premium features locked until checkout opens rather than free in the meantime; making checkout unavailable in the BFF as well was passed over because a purchase that got through would be a real, valid subscription, not a fault, and reusing `PAYMENTS_ENABLED=false` was rejected because it hides the Premium page the announcement lives on.
[2026-09-14] fantasy-web: the free plan's zero on the Premium page is Paddle's own `formattedTotals.discount` from the same price preview (the discount on a price nobody discounted), with "Free" when the preview is missing or carries a discount — formatted with `Intl` in the browser's language it read "SEK 0" beside Paddle's "49.00 kr" on an English browser; deriving the zero by rewriting the digits in Paddle's total was passed over as parsing a string Paddle may reformat, and formatting in a fixed locale as guessing at a style Paddle chooses per currency.
[2026-09-14] fantasy-web: `index.html` carries a `<noscript>` summary of the product, both plans and links to pricing, terms, privacy, refunds and the contact address, and nginx answers a POST to a page with the page instead of 405 — Paddle's domain review rejected the live site twice as "offline or under construction", and the only unknown clients after the resubmission ran no JavaScript (one POSTed to `/` four times and got 405, one fetched `/` and got a page with only a title); prerendering the public pages with Angular SSR was passed over for now as a much larger change touching ~18 files that use browser APIs, and a static block inside `<app-root>` because every visitor would see it flash before the app starts. The summary repeats wording from the landing page, the Premium page and the footer, so it has to follow them, and it names no price because Paddle localizes it.
[2026-09-15] fantasy-web: the public pages (`/`, `/premium`, `/terms`, `/privacy`, `/refunds`) are prerendered at build time with `outputMode: "static"`, and every other route stays client-rendered from `index.csr.html` — this takes up the prerendering the line above passed over, because Paddle rejected the site a third time with the `<noscript>` summary live, and an HTML-to-text fetch (the kind an automated review uses) drops `<noscript>` and still read only the title, on `/terms` too, where Paddle checks for the merchant of record. No hydration: the browser renders over the prerendered page as it rendered over the empty one, so no component had to become hydration-safe. While prerendering nothing reads storage, no analytics or Sentry starts, and the only API call allowed is `/api/v1/features`; the landing demo's players and the visitor's Paddle price load in the browser, so no stale data is baked in. Server rendering per request was passed over because a static file needs no Node server, and baking a Paddle base price in at build time was left for later.
[2026-09-15] fantasy-web: `EntitlementService.refresh()` asks nothing unless an account is signed in, rather than the Premium page checking before it calls — a signed-out visit to `/premium` sent the read without a token and logged a 401 in the console, and the page calls `refresh()` from four places (arrival, retry, the 409 path, the back-from-checkout poll), so a check in `ngOnInit` as proposed would have fixed one and left the rule for the next caller to forget; the service is the only place that sends the request, and a signed-out visitor is already in the idle state `forget()` leaves (#658).
[2026-09-15] fantasy-web: a failed E2E run uploads only its failure screenshots, and CI writes neither an HTML report nor a trace — the repo went public, any signed-in GitHub user can download a run's artifacts, and Playwright titles a fill step `Fill "<value>"` in both, so the signed-in specs put the staging account's password in a public file whenever one failed (it had, on 2026-09-11, while the repo was still private); signing in through the API with a stored state was passed over because the report would still carry whatever the next spec types, and Playwright has no masking for a fill.
[2026-09-15] fantasy-web: the projection table hides rows for players who left the pool without saying how many ("344 players are no longer in the league…"), and `droppedPlayerCount` is gone with the notice — Alexander's call, since nearly everyone who leaves a pool is a player no fantasy manager would draft, so the count read as a warning about nothing; keeping the count and showing it only above a threshold, or only for players who had points, was passed over as a rule to tune for a line nobody acts on.
[2026-09-15] fantasy-web: the player tables draw no headshot beside the names while no player in the list has a picture (`hasHeadshots`, decided per table and not per row), rather than reading the BFF's `PLAYER_AVATARS_ENABLED` through `GET /api/v1/features` — the BFF already sends every headshot as null when it is off, so the data answers the question with no API change, no second request and no flag to keep in step, and it also covers a pool that has no pictures for any other reason; hiding the circle per row was rejected because with pictures on roughly one player in seven has none, and his name would stop lining up with the rest. The draft board follows the same rule over its pool (`DraftPlayerLookupService.showAvatars`): its position-coloured `app-player-avatar` hides itself rather than each of the six places that draw one deciding again, and the available-players grid drops the avatar's column, since a hidden grid item would otherwise leave every name in the avatar's 2rem slot. The colour goes with it, so every panel now names the position in words: the pick feed and both results views carry the same coloured chips the available list and the roster had, drawn by one `app-position-chips` rather than a third and fourth copy of the markup and CSS those two panels each held, since the avatar's colour had been the only sign of a pick's position there.
[2026-09-15] fantasy-web: the player tables draw no headshot beside the names while no player in the list has a picture (`hasHeadshots`, decided per table and not per row), rather than reading the BFF's `PLAYER_AVATARS_ENABLED` through `GET /api/v1/features` — the BFF already sends every headshot as null when it is off, so the data answers the question with no API change, no second request and no flag to keep in step, and it also covers a pool that has no pictures for any other reason; hiding the circle per row was rejected because with pictures on roughly one player in seven has none, and his name would stop lining up with the rest. The draft board follows the same rule over its pool (`DraftPlayerLookupService.showAvatars`): its position-coloured `app-player-avatar` hides itself rather than each of the six places that draw one deciding again, and the available-players grid drops the avatar's column, since a hidden grid item would otherwise leave every name in the avatar's 2rem slot. The colour goes with it: the available list and the roster still show the position as chips, but in the pick feed and the results the avatar's colour was the only sign of a player's position.
[2026-09-16] fantasy-web: the prerendered `/premium` quotes Paddle's base price, asked of Paddle's REST pricing preview for the US at build time, and a Docker build that sells Premium fails if the page comes out without one — Paddle refused slapstat.com a fourth time, and missing public pricing is the reason its own docs name first, while the page a no-JavaScript reader got said only "confirmed at checkout"; a price typed into a build arg was passed over because it would drift from the catalog unnoticed, and the browser still replaces the quote with the visitor's local price. `/pricing` and `/account` are redirected by nginx as well as by the router, since the router's redirect needs the scripts that reader does not run.
[2026-09-16] fantasy-web: a build that sells Premium only warns when the prerendered `/premium` has no price, until `PREMIUM_PRICE_REQUIRED=true` makes it fail again — v0.205.0's prod image failed the check because Paddle's live pricing preview gave no quote while the domain review is pending, and the page that check protects exists to pass that review; dropping the check was passed over because it has to come back the day Paddle approves, and a build arg lets that be a Coolify variable rather than another release.
[2026-09-16] fantasy-web: the prerendered `/premium` states the price from the `PREMIUM_BASE_PRICE_USD` build arg, and the REST pricing preview of #671 and `PREMIUM_PRICE_REQUIRED` of #673 are gone — Paddle's REST API answers a client token with 403 `authentication_malformed` (seen in the staging build log, #674), so no build-time quote is possible without the secret API key, which a build arg would print into Coolify's log; calling the endpoint Paddle.js uses internally was rejected as undocumented, and the arg was Alexander's call, knowing it must be changed by hand with the catalog price. A build that sells Premium without the arg fails rather than warns.
[2026-09-16] fantasy-web: link-preview crawlers are rate-limited per client in `nginx.conf` (30 previews and 20 cards a minute, keyed on the last X-Forwarded-For entry Traefik appends), leaving the BFF's shared buckets as a ceiling on total load — nginx reaches the BFF through its public origin, so the BFF sees only this server; routing nginx to the BFF over the Docker network was passed over because the BFF has no network alias and the web no build arg for one (a Coolify change), and a header naming the client for the BFF to trust would need a shared secret the web does not hold.
[2026-09-16] fantasy-web: the E2E suite keeps its daily schedule rather than running after every merge, even though Actions minutes are free since the repo went public — a merge-triggered run would have to start after `tag-on-merge` finishes the staging deploy (a `workflow_run` chain, since the redeploy wait was removed in #389), every merge would leave more throwaway accounts on staging, and a web-only trigger would still miss breakage merged in bff, so the daily run plus a manual dispatch after a risky merge stays the better trade.
[2026-09-16] fantasy-web: "Sign out" in the account menu still ends only this browser's session, and "Sign out everywhere" is a separate button on the Profile page (fantasy-bff#252) — the only server-side revoke ends every session the account has, so wiring it into the everyday sign-out would log someone out of their phone each time they left a laptop; a confirm dialog on the new button was passed over because signing back in is the whole cost of pressing it by mistake.
[2026-09-16] fantasy-web: a Yahoo connect is claimed in `landingRedirectGuard`, from the one-time code the callback puts in the URL fragment, and a user's own connect still ends silently on /projections while only the admin panel explains a failed claim — a dedicated claim route was rejected because the callback's destination is the `WEB_POST_CONNECT_URL` env on yahoo-service and changing it is an infra step, and a banner on /projections was left out to keep the fix to the binding.
[2026-09-16] fantasy-web: nginx runs entirely as `nginx`, master included, and still listens on 80 because Docker sets `net.ipv4.ip_unprivileged_port_start=0` inside a container (seen on both hosts, Docker 29.5.3) — moving to `nginxinc/nginx-unprivileged` on 8080 was rejected because it changes the port Coolify routes to in both environments, an infra step outside the repo, and keeping the stock root master with `nginx` workers was rejected as leaving root in the container. Host networking would break this.
[2026-09-17] fantasy-web: Premium is sold through Stripe-hosted Checkout, so `/pay`, Paddle.js, `@paddle/paddle-js` and the `PADDLE_CLIENT_TOKEN`/`PADDLE_PRICE_ID` build args are gone and `/premium` quotes `PREMIUM_BASE_PRICE_USD` in the browser as well as prerendered (Paddle refused the live account for good) — a local-currency price on the page was dropped because Stripe offers no browser price preview without loading Stripe.js on a public page and a price API in the BFF, while Checkout's Adaptive Pricing converts at payment; `worker-src blob:` stays in the CSP because a refused worker fails silently and nothing proved it unused.
[2026-09-17] fantasy-web: a preset whose board was seeded but whose draft setup was never saved stays on offer and Start reopens that board, rather than the board being deleted and the preset reseeded — reseeding would give fresher model numbers but deletes a board another open tab may be setting up, and an in-progress preset draft already keeps the numbers it was seeded with; creating the board only once setup is saved was passed over because the draft page is routed by the board's id.
[2026-09-17] fantasy-web: a preset draft's board is created by the draft page when its setup is confirmed (`/draft/new/:preset` until then), not by Start on the draft start page — creating it first left an empty board behind every setup someone backed out of, and reusing that board (#688) still kept it forever with the model numbers of the day it was made; a cleanup job for the empty boards was rejected as a moving part that has to be seen firing, when not creating them removes the class; a league sync made during the setup is held in memory until then, and db-service V24 deletes the boards already left behind.
[2026-09-17] fantasy-web: a user's Yahoo connect returns to the page it started from (path and query, remembered in the tab's sessionStorage at the click and redeemed once by `landingRedirectGuard`), still ending on /projections when nothing was remembered — carrying the path through the signed OAuth `state` was passed over because it needs yahoo-service, the BFF and both pinned specs to change for a value the browser already holds, and it would need the same open-redirect check server-side; no `?yahoo=` params are added to the returned page since only /admin reads them.
[2026-09-17] fantasy-web: the league a draft is ranked by (points/category, stats, weights, size, roster, Yahoo/ESPN import) is set on the draft picker, above the preview whose weight row becomes editable, and belongs to the draft: it travels to the draft page in history state and is saved as `draft.settings`, and the draft page reads and writes only that, so neither the picker nor the draft page ever changes the projection a draft is played against — Alexander's call, over first saving it into the board (which made editing a draft's scoring silently rescore the user's projection); a draft saved before this has no `settings` and is ranked by the projection's until its next save copies them. Putting the controls only in the draft-setup step was passed over because it has no preview to show what a weight does. The Import/Synced button the editor and Who's hot each copied is now one `app-league-import-button`.
[2026-09-17] fantasy-web: the refund policy is a section of the terms again (`/terms#refunds`, with `/refunds` answered by a 301 in nginx and a redirect route, and the footer's Refunds link kept), at Alexander's request — this reverses the separate page of the 2026-09-11 line, which existed because Paddle's live checklist asked for one; Paddle is gone and Stripe asks only that the policy be reachable. The router enables `anchorScrolling` so the in-app link lands on the section.
[2026-09-17] fantasy-web: the site icon Google Search can show is `icon-192.png`, declared `sizes="192x192"` beside the ICO, which now declares all three of its sizes (16, 32, 48) instead of claiming 32 — Google asks for a square icon of at least 48px in multiples of 48, and the only icon the page declared said 32, so Search showed a blank globe while the file itself held a 48px image; replacing the ICO was passed over because older browsers and the default /favicon.ico request still want it, and an SVG icon because Google's guidance names no SVG preference and the PNG already existed unused.
[2026-09-17] fantasy-web: signing in, a signed-in visit to `/` and a Yahoo connect with nowhere remembered now land on `/home` rather than `/projections`, and a user with landing-demo work waiting is handed on to `/projections` by `demoRedemptionGuard`, since that list is what saves it — the checklist there has two steps (a projection, a started draft) and not the "sync your league" step the sketch proposed, because the projection list the BFF serves does not say whether a board is synced, and loading every board's settings to find out was passed over as far too heavy for a landing page; improving the empty state on `/projections` alone was passed over because it gives a returning user nothing.
[2026-09-17] fantasy-web: the footer and the `<noscript>` summary no longer link Refunds; the refund policy is reached through Terms, where it is a section, at Alexander's request — `/refunds` still redirects to `/terms#refunds` for links already out there, and the router's `anchorScrolling`, added only for the footer link, is gone with it.
[2026-09-17] fantasy-web: a Yahoo connect started in the import dialog reopens that dialog on Yahoo when the claim returns to its page, through a one-shot in-memory hand-back in `YahooConnectReturnService` (`take` records the page, the page's `returnedTo(router.url)` answers true once), not by putting the dialog's open state in the URL (`?import=yahoo`) — a query param would have to be written and removed on every open and close across three pages and would add history entries, and a sessionStorage flag was passed over because it outlives the round trip and could reopen the dialog on a later reload.
[2026-09-17] fantasy-web: a projection is imported from a spreadsheet in the browser (paste, .csv/.tsv, or .xlsx read by `read-excel-file`'s universal build, loaded on demand), not uploaded to the BFF, and Google Sheets is reached by copy-paste or its .xlsx download rather than the Sheets API — nothing needs storing server-side and the stats reach the server through the ordinary autosave, while the Sheets API would add an OAuth scope Google has to review; SheetJS (`xlsx`) was rejected because npm only carries its old, vulnerable release, `exceljs` for its size, and the browser build of `read-excel-file` because it parses in a Web Worker the CSP would have to allow for no gain at this file size. Columns are guessed and shown for correction before anything changes, only mapped stats of matched players are written (never rescaled by games), and the import is free (Alexander's call). It ships dark behind the `SPREADSHEET_IMPORT_ENABLED` build arg, off by default, at Alexander's request; a flag in the BFF's `GET /api/v1/features` was passed over because that one exists so the web cannot offer what the BFF's endpoints refuse, and no endpoint serves this.
[2026-09-17] fantasy-web: a spreadsheet import squares the lines it writes (`spreadsheet-reconcile.ts`: parts win over their total, an imported total without parts scales the line's own split or, for PP/SH points it had none of, the player's goals-to-assists split, rates follow counts, defencemen points follow points) rather than loosening the table's warnings or leaving them — a sheet rounds each column separately and gives totals without parts, so every imported row showed a warning the user did not cause, while the AI projection already derives its totals from their parts (checked in projection-service and the BFF) and needed no change; a name that still fits several pool players after club and position is left for the user to pick in the dialog rather than guessed.
[2026-09-17] fantasy-web: a spreadsheet name that differs from the pool's spelling is matched by rule, not by a list of players — a first-name form table (Tommy/Thomas, Nick/Nicholas) and transliteration folding (y/j/i, kh/h, x/ks, ye/e: Yegor/Egor, Dorofyev/Dorofeyev), then a one-or-two-letter edit distance — and every such match is listed in the dialog as "matched by a different spelling" with Leave out, instead of silently accepted or sent to the not-found list; a per-player alias table was rejected because it would miss every prospect nobody had written in, and a looser fuzzy threshold because retired players (Kopitar, Atkinson) must stay unmatched rather than land on a stranger.
[2026-09-17] fantasy-web: the new-projection page sets the league before Create with the draft picker's own controls, moved to `shared/league-settings-controls`, rather than a trimmed copy of them or a step after the projection exists — a second copy would drift from the draft picker's, and a league set only afterwards leaves the preview scored by defaults, which reads as "not my league" at the moment of deciding to create; a copied board is laid over only when its league was changed, so an untouched copy is still sent byte-exact.
[2026-09-17] fantasy-web: `/home` shows the features as equal cards (Projections, Draft mode, AI projection, Who's Hot) with a share-link row under them, not the two-step "Draft prep" checklist it shipped with the same day — Alexander's call: a checklist says there is one right order, and there is not (some go straight to Draft mode from a preset, some start from the AI projection, some import a shared board and draft with it). "Create projection" is still the one filled navy button, so the common path is the most visible without being named the first step; the list of further projections is folded into a "My projections (n)" link, since the full list is one click away.
[2026-09-17] fantasy-web: a popover closes on Enter in a number or text field (in `PopoverTriggerDirective`, so every menu gets it), instead of gaining a close button in its corner as first proposed — the fields save as they are typed, so nothing said the value was in and the only way out after typing was a click elsewhere, which a cross would not have changed; a cross also belongs to dialogs that cover the page, not to a small non-modal menu that already closes on Escape, its own trigger and an outside click. A search box is left out, because the list under it is what the user is after.
[2026-09-17] fantasy-web: the second action in a `/home` card ("My projections (n)", the AI card's "Open Draft mode") is a secondary button, not the underlined text link it shipped as — the same "Open Draft mode" was a button on the Draft mode card and a link on the AI card, and getting back to existing projections is what a returning user does most, so it should not read as a footnote; two filled buttons was rejected because "Create projection" stays the one dominant action, and the navy card gets an outline `btn-on-dark` because `btn-secondary`'s white surface would outweigh the gold button beside it.
[2026-09-17] fantasy-web: navigation and footer links are title case (Send Feedback, Sign In, Get Started), and Draft Mode and My Projections are title case wherever they appear, while buttons, headings and labels elsewhere stay sentence case — Alexander's call, over the recommendation to keep sentence case throughout: the two names read as product names like Who's Hot, and the nav and footer as a set of named destinations; the cost is that a page name can differ from a sentence-case button beside it, so COPY-RULES.md now blocks "Draft mode" and "My projections" case-sensitively.
[2026-09-17] fantasy-web: every column's decimals are set from one "Decimals" menu in the table toolbar, whose top row sets the counting stats together and leaves SH%, GAA, SV% and Win% alone, rather than an "apply to all" button in each column's menu — one number for every column prints a .915 save percentage as 0.9, and a button inside a column menu neither shows what the columns are set to nor is where anyone looks for a table-wide setting; the choices are four buttons (0 to 3) per row instead of a number field, so any change is one click, and the column menu's own field stays because it edits the same setting. "Reset to defaults" is the way back that went missing with the settings panel: one decimal set by hand turns `useDefaultDecimals` off for every column, and decimals are not in the undo history.
[2026-09-17] fantasy-web: on the new-projection page and the draft picker the presets share one league, while each board keeps its own — the league was filed per preset, so a Yahoo/ESPN import made with "Last season's stats" picked fell back to the defaults on switching to the AI projection, though the presets differ only in their numbers and open on the same league; one page-wide league that also overrides a picked board was passed over because it would lay the import over a board's own league and break the byte-exact copy #62 keeps.
[2026-09-17] fantasy-web: the "Decimals" button sits at the end of the search/filter row, not in the league group beside Stats and Import league — Alexander's call: that group is what the projection is scored on and which league it came from, and decimals only change how the numbers are printed; the menu names a stat by its abbreviation with the full name behind an info tip (`app-help-tip`) instead of spelled out beside it, to save the width.
[2026-09-17] fantasy-web: a Yahoo/ESPN import on the new-projection page or the draft picker applies to every starting point, copies and boards included, while a hand edit stays with the card it was made on and a new import clears the edits before it — Alexander's call, reversing the half of the previous line that kept an import off boards; one page-wide league for hand edits too was passed over because tweaking a preset would silently rewrite a board's own league.
[2026-09-17] fantasy-web: the deployed apps keep the recent builds' scripts and stylesheets on a volume the old and new container share (`publish-assets.sh`, a file stays while one of the last 3 builds lists it or it was deployed within 14 days), rather than a delayed second reload when a chunk fails to load — Coolify's rolling update has Traefik answer from both containers for a few seconds, and a reload only rolls the dice again, while every tab open across a deploy was also forced to reload on its next click; a CDN or object store for the assets does the same job with one more service to run.
[2026-09-17] fantasy-web: a spreadsheet import is an imported board created from the Imports tab (and the home page's import panel), not a button in the editor that writes into the open projection — Alexander's call, since a projection kept in a spreadsheet is a starting point like a shared board; it is created with `kind: imported` and no origin (fantasy-bff#266 made the BFF honour that kind), with every pool player on an empty line unless the sheet names him, so the board is the sheet's numbers rather than a mix with last season's; the icon is Lucide's file-spreadsheet in the success green, not Excel's or Google Sheets' logo, which are trademarks and would read as an endorsement.
[2026-09-17] fantasy-web: following a Yahoo draft polls `GET /yahoo/leagues/{key}/draft` every 5 s and replaces the board's teams, order and picks with the league's, locking pick edits while it runs, and the switch is the BFF's `leagueDraftSync` rather than a build flag — rejected: merging league picks into a hand-entered board, which cannot tell a pick entered here from the same player taken there and would number picks differently from the league; and a server push (SSE/websocket), since the BFF itself can only poll Yahoo, so a push would add a connection to hold open without making a pick arrive sooner.
[2026-09-17] fantasy-web: the projection editor and Who's hot draw a player's positions as the draft board's coloured chips rather than the plain "(C, LW)" / "(C/LW)" after the name (a skater Who's hot cannot find in the pool gets no chip where it showed "—") — the chips move to `shared/position-chips` taking the positions themselves, and the draft board keeps a thin `app-player-position-chips` that looks them up by id, rather than the editor injecting `DraftPlayerLookupService` (a draft-pool service it has no pool for) or keeping a second copy of the markup; the editable trigger rings the chip group on hover and marks an owner's correction with a dashed outline, since the old underline cannot reach inside the chips.
[2026-09-17] fantasy-web: on a board imported from a spreadsheet a player the sheet names starts from his own pool line under the sheet's stats, and only players it leaves out start empty — every-line-empty (web#745) left each named row with the stats the sheet has no column for at zero, so 50 goals beside 0 shots warned on every row the staging E2E opened; the empty start is kept for unnamed players so the board still ranks only what the sheet projects.
[2026-09-17] fantasy-web: the streamer planner (`/streamer-planner`) is switched by the BFF's `streamerPlanner` on `/api/v1/features`, which its route guard asks directly and the nav reads through `FeatureService` (fantasy-web#751) — a `STREAMER_PLANNER_ENABLED` build arg like Who's Hot's was rejected because it needs a rebuild to flip and can disagree with the server; the guard fetches rather than reading `FeatureService` because that answer may not have landed on a first navigation, and a false there would bounce a user the server would serve.
[2026-09-17] fantasy-web: the share-link field and the spreadsheet button are one `app-projection-import` with an "or" rule between them, not the pair written out on each of the three pages that offer both — nothing said the two were alternatives rather than a field and then a button, and the "or" has to vanish with `SPREADSHEET_IMPORT_ENABLED` off, which is one condition here instead of three; the rule stops at the link field's width rather than the panel's, since across a 1100px page it cut the page in half and separated more than the two controls.
[2026-09-17] fantasy-web: pressing Create projection or Draft Mode on a shared projection while signed out navigates to /register, rather than revealing a note beside the buttons holding Sign in and Register links — the note asked a visitor who had already decided to read a sentence and decide again, and both of its links led to a form anyway; opening the form in a new tab was rejected because the copy resumes through a returnUrl round trip, which a second tab would strand behind a stale first one, and /login was rejected as the destination because the visitor the page has to win is the one without an account, who reaches the sign-in form in one click from the register form's footer with the returnUrl preserved.
[2026-09-17] fantasy-web: a /s/<token>?action=… opened without a session drops the action and renders the board, instead of resuming it — resuming now means a redirect to the register form, so a share link someone passed on with the action still on it would bounce every signed-out reader to a signup page without ever showing them the projection it points at.
[2026-09-17] fantasy-web: the streamer planner's free agents are ranked in the browser, by the chosen league's own scoring settings through the same `ProjectionRankingService` a projection uses, and the league is remembered in localStorage rather than on a projection (#751) — ranking on the server was rejected because the answer could not then be re-weighted per league, and hanging the league off a projection was rejected because the planner is about a league's wire and a manager may not have built a projection for it; `minGoalieGames` is zero here, since a season minimum meant to keep backups off a table would disqualify every goalie in a one-week view.
[2026-09-17] fantasy-web: a picked profile picture goes through a crop dialog (drag, zoom, arrow keys) before it is uploaded, and the crop lives in the picture's own pixels in `models/avatar-crop` rather than as a CSS transform — the app took the middle square and said nothing, which puts the subject of a wide photo at the edge of the circle or half out of it, and since only the 256px square is ever stored (the file is not kept anywhere) a bad guess could not be nudged afterwards, only replaced with another guess; biasing the default crop towards the top of a portrait was rejected as a second guess nobody can see, and a server-side crop from a stored original was rejected because it would mean uploading and keeping the full photo to fix the framing of a 32px circle.
[2026-09-17] fantasy-web: a press on a shared board that needs an account is written down in sessionStorage (`shared_copy_intent`, one board and one destination, taken once on the way back in) instead of riding on the return URL as `?action=draft` — this supersedes both 2026-09-17 lines above: the parameter made the press something a URL could assert, so `/s/<token>?action=draft` sent to a signed-in reader silently put a copy of a board in their account and dropped them into a draft against it, and the guard the earlier line added only closed that for signed-out readers. Rejected: keeping the parameter and pairing it with a one-time nonce in sessionStorage, which defends the same thing with a URL and a store rather than a store; and a time-to-live on the stored press, since it is per tab, spent on the first board that asks and dies when the tab closes.

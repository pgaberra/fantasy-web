openapi: 3.1.0
info:
  title: Fantasy Hockey BFF API
  description: Backend for Frontend aggregation layer for the Fantasy Hockey application
  version: "1.0"
servers:
- url: /
security:
- bearerAuth: []
tags:
- name: Authentication
  description: Register and login endpoints
- name: Admin
  description: "Admin-only: Yahoo service account + player sync"
- name: Yahoo
  description: The authenticated user's Yahoo fantasy account and leagues
- name: Players
  description: "NHL player stats endpoints (public, read-only)"
- name: Projections
  description: The authenticated user's saved player projections
- name: Projection model
  description: "Model-generated projection lines, and measured totals over a range\
    \ of games. Data © MoneyPuck.com, used under its non-commercial terms."
- name: Versions
  description: Deployed version and reachability of every service
- name: ESPN
  description: The authenticated user's ESPN fantasy leagues
- name: Billing
  description: "Subscription checkout, customer portal and premium entitlements"
paths:
  /api/v1/projections/{id}:
    get:
      tags:
      - Projections
      summary: "Fetch one of the current user's saved projections, including its data"
      operationId: get
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "200":
          description: Projection found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "404":
          description: No such projection for this user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
    put:
      tags:
      - Projections
      summary: Update one of the current user's saved projections
      operationId: update
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectionRequest"
        required: true
      responses:
        "200":
          description: Projection updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "400":
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "404":
          description: No such projection for this user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "409":
          description: Another projection with that name already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
    delete:
      tags:
      - Projections
      summary: Delete one of the current user's saved projections
      operationId: delete
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        "204":
          description: Projection deleted
        "404":
          description: No such projection for this user
  /api/v1/espn/credentials:
    get:
      tags:
      - ESPN
      summary: Whether the user has stored ESPN cookies (for private leagues)
      operationId: credentialStatus
      responses:
        "200":
          description: Status returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CredentialStatusResponse"
    put:
      tags:
      - ESPN
      summary: Store (or replace) the user's ESPN cookies
      operationId: saveCredentials
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EspnCredentialsRequest"
        required: true
      responses:
        "204":
          description: Cookies stored
        "400":
          description: Missing or invalid cookie values
    delete:
      tags:
      - ESPN
      summary: Delete the user's stored ESPN cookies
      operationId: deleteCredentials
      responses:
        "204":
          description: Cookies deleted (or none existed)
  /api/v1/yahoo/connect:
    post:
      tags:
      - Yahoo
      summary: Start connecting the user's Yahoo account
      description: Returns the Yahoo consent URL the web app should redirect the browser
        to.
      operationId: connect
      responses:
        "200":
          description: Authorization URL created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizeUrlResponse"
  /api/v1/projections:
    get:
      tags:
      - Projections
      summary: List the current user's saved projections (metadata only)
      operationId: list
      responses:
        "200":
          description: "The user's projections, newest first"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectionSummaryResponse"
    post:
      tags:
      - Projections
      summary: Save a new projection for the current user
      operationId: create
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectionRequest"
        required: true
      responses:
        "201":
          description: Projection created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "400":
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
        "409":
          description: A projection with that name already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectionResponse"
  /api/v1/billing/portal-session:
    post:
      tags:
      - Billing
      summary: Open the billing management portal
      description: Returns the provider-hosted customer-portal URL for managing or
        cancelling the subscription.
      operationId: portalSession
      responses:
        "200":
          description: Portal URL created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortalUrlResponse"
        "404":
          description: "Payments not enabled, or the user has no billing customer"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortalUrlResponse"
  /api/v1/billing/checkout-session:
    post:
      tags:
      - Billing
      summary: Start a checkout session to subscribe
      description: Returns the provider-hosted checkout URL the web app should redirect
        the browser to.
      operationId: checkoutSession
      responses:
        "200":
          description: Checkout URL created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutUrlResponse"
        "404":
          description: Payments are not enabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutUrlResponse"
  /api/v1/auth/verify:
    post:
      tags:
      - Authentication
      summary: Verify an email address with a token
      description: Consumes a single-use verification token from the email link and
        marks the account's email verified.
      operationId: verifyEmail
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyEmailRequest"
        required: true
      responses:
        "200":
          description: Email verified
        "400":
          description: "Invalid or expired token, or validation error"
  /api/v1/auth/verify/resend:
    post:
      tags:
      - Authentication
      summary: Resend the verification email
      description: "Always returns 200. To avoid revealing whether an account exists,\
        \ the response is identical whether or not a verification email was sent."
      operationId: resendVerification
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResendVerificationRequest"
        required: true
      responses:
        "200":
          description: Request accepted (an email is sent only if a matching unverified
            account exists)
        "400":
          description: Validation error
  /api/v1/auth/register:
    post:
      tags:
      - Authentication
      summary: Register
      description: Create a new user account and return an authenticated session
      operationId: register
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
        required: true
      responses:
        "201":
          description: User registered and logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Email already in use or validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/auth/refresh:
    post:
      tags:
      - Authentication
      summary: Refresh tokens
      description: Exchange a valid refresh token for a new access token and refresh
        token pair
      operationId: refresh
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
        required: true
      responses:
        "200":
          description: Tokens refreshed successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid or expired refresh token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/auth/password/reset:
    post:
      tags:
      - Authentication
      summary: Reset password with a token
      description: Consumes a single-use reset token from the email link and sets
        a new password.
      operationId: resetPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordRequest"
        required: true
      responses:
        "200":
          description: Password updated
        "400":
          description: "Invalid or expired token, or validation error"
  /api/v1/auth/password/forgot:
    post:
      tags:
      - Authentication
      summary: Request a password reset email
      description: "Always returns 200. To avoid revealing whether an account exists,\
        \ the response is identical whether or not a reset email was sent."
      operationId: forgotPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordRequest"
        required: true
      responses:
        "200":
          description: Request accepted (an email is sent only if a matching password
            account exists)
        "400":
          description: Validation error
  /api/v1/auth/login:
    post:
      tags:
      - Authentication
      summary: Login
      description: Authenticate with email and password to receive a JWT
      operationId: login
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
        required: true
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/auth/google:
    post:
      tags:
      - Authentication
      summary: Login with Google
      description: "Verify a Google ID token and log the user in, registering a new\
        \ account on first login"
      operationId: googleLogin
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GoogleLoginRequest"
        required: true
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid Google ID token or unverified email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/auth/google/code:
    post:
      tags:
      - Authentication
      summary: Login with a Google authorization code
      description: "Exchange a Google OAuth authorization code (from the top-level\
        \ redirect flow) for an ID token server-side and log the user in, registering\
        \ a new account on first login. Used by browsers that block the embedded Google\
        \ Sign-In button, such as iOS Safari."
      operationId: googleCodeLogin
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GoogleCodeLoginRequest"
        required: true
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: "Invalid or expired authorization code, or unverified email"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Unrecognized redirect URI or validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/auth/facebook:
    post:
      tags:
      - Authentication
      summary: Login with Facebook
      description: "Verify a Facebook access token and log the user in, registering\
        \ a new account on first login"
      operationId: facebookLogin
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FacebookLoginRequest"
        required: true
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid Facebook access token or missing email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
  /api/v1/admin/yahoo/connect:
    post:
      tags:
      - Admin
      summary: Start connecting the Yahoo service account
      description: Returns the Yahoo consent URL to send the admin's browser to. The
        resulting token is stored under the reserved service-account id.
      operationId: connectYahooServiceAccount
      responses:
        "200":
          description: Authorization URL created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizeUrlResponse"
  /api/v1/admin/player/sync:
    post:
      tags:
      - Admin
      summary: Trigger a player sync
      description: Refreshes the cached player read model from Yahoo.
      operationId: triggerPlayerSync
      responses:
        "200":
          description: Sync triggered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncAcceptedResponse"
  /api/v1/yahoo/leagues:
    get:
      tags:
      - Yahoo
      summary: List the user's NHL fantasy leagues
      operationId: leagues
      responses:
        "200":
          description: Leagues returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaguesResponse"
        "404":
          description: User has not connected Yahoo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaguesResponse"
  /api/v1/yahoo/leagues/{leagueKey}/teams:
    get:
      tags:
      - Yahoo
      summary: List a league's teams (names + which is the user's own)
      operationId: teams
      parameters:
      - name: leagueKey
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Teams returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueTeamsResponse"
        "404":
          description: User has not connected Yahoo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueTeamsResponse"
  /api/v1/yahoo/leagues/{leagueKey}/projection-settings:
    get:
      tags:
      - Yahoo
      summary: Get a league's settings mapped to projection settings
      description: "Maps the Yahoo league's scoring basis (points vs category), active\
        \ stat columns, point weights, roster slots and size into the projection's\
        \ own shape."
      operationId: projectionSettings
      parameters:
      - name: leagueKey
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Mapped settings returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueProjectionSettingsResponse"
        "404":
          description: User has not connected Yahoo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueProjectionSettingsResponse"
  /api/v1/yahoo/connection:
    get:
      tags:
      - Yahoo
      summary: Whether the user has connected their Yahoo account
      operationId: connection
      responses:
        "200":
          description: Connection status returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectionResponse"
  /api/v1/versions:
    get:
      tags:
      - Versions
      summary: Get service versions
      description: Returns the deployed version and reachability of the BFF and each
        downstream service
      operationId: getVersions
      responses:
        "200":
          description: Versions retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionsResponse"
  /api/v1/projection-model/splits/skaters:
    get:
      tags:
      - Projection model
      summary: Skaters' measured totals over the closing games of a season
      description: "What players actually produced over a stretch of their team's\
        \ schedule — measured, not projected. Ranges are in team game numbers, so\
        \ the same range covers the same stretch for every player; one who missed\
        \ some of them shows fewer games."
      operationId: skaterSplits
      parameters:
      - name: season
        in: query
        required: false
        schema:
          type: integer
          format: int32
      - name: lastGames
        in: query
        description: How many of the team's final games to cover
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 84
          minimum: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 100
          maximum: 500
          minimum: 1
      responses:
        "200":
          description: "Totals over the range, highest scoring first"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlayerSplitResponse"
  /api/v1/projection-model/splits/goalies:
    get:
      tags:
      - Projection model
      summary: Goalies' measured totals over the closing games of a season
      operationId: goalieSplits
      parameters:
      - name: season
        in: query
        required: false
        schema:
          type: integer
          format: int32
      - name: lastGames
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 20
          maximum: 84
          minimum: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 100
          maximum: 500
          minimum: 1
      responses:
        "200":
          description: Totals over the range
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlayerSplitResponse"
  /api/v1/projection-model/seed:
    get:
      tags:
      - Projection model
      summary: Generate projection lines for every mapped player
      description: "Returns the model's projected stat lines keyed by this platform's\
        \ player id, ready to be saved as a new projection. Scoring settings are not\
        \ included — those belong to the user's league, so the client supplies them\
        \ when saving."
      operationId: seed
      parameters:
      - name: season
        in: query
        description: Season to project; defaults to the configured one
        required: false
        schema:
          type: integer
          format: int32
      responses:
        "200":
          description: Generated lines plus a summary of what was skipped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SeededProjectionResponse"
  /api/v1/players/skaters:
    get:
      tags:
      - Players
      summary: Get all skaters
      description: Returns all skaters with full season stats
      operationId: getSkaters
      responses:
        "200":
          description: Skaters retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SkaterResponse"
        "502":
          description: NHL service unavailable
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SkaterResponse"
  /api/v1/players/goalies:
    get:
      tags:
      - Players
      summary: Get all goalies
      description: Returns all goalies with full season stats
      operationId: getGoalies
      responses:
        "200":
          description: Goalies retrieved successfully
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GoalieResponse"
        "502":
          description: NHL service unavailable
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GoalieResponse"
  /api/v1/espn/leagues/{leagueId}/teams:
    get:
      tags:
      - ESPN
      summary: List an ESPN league's teams (names + which is the user's own)
      operationId: teams_1
      parameters:
      - name: leagueId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Teams returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EspnLeagueTeamsResponse"
        "400":
          description: League is private (cookies missing/invalid) or the league id
            is malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EspnLeagueTeamsResponse"
        "404":
          description: No such ESPN league for that id in the current or previous
            season
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EspnLeagueTeamsResponse"
  /api/v1/espn/leagues/{leagueId}/projection-settings:
    get:
      tags:
      - ESPN
      summary: Get an ESPN league's settings mapped to projection settings
      description: "Maps the ESPN league's scoring basis (points vs category), active\
        \ stat columns, point weights, roster slots and size into the projection's\
        \ own shape."
      operationId: projectionSettings_1
      parameters:
      - name: leagueId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Mapped settings returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueProjectionSettingsResponse"
        "400":
          description: League is private (cookies missing/invalid) or the league id
            is malformed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueProjectionSettingsResponse"
        "404":
          description: No such ESPN league for that id in the current or previous
            season
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeagueProjectionSettingsResponse"
  /api/v1/billing/entitlements:
    get:
      tags:
      - Billing
      summary: The current user's premium entitlement
      description: Live read of whether the user's subscription currently grants premium
        access.
      operationId: entitlements
      responses:
        "200":
          description: Entitlement returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementsResponse"
  /api/v1/admin/yahoo/connection:
    get:
      tags:
      - Admin
      summary: Whether the Yahoo service account is connected
      operationId: yahooServiceConnection
      responses:
        "200":
          description: Connection status returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectionResponse"
  /api/v1/admin/player/sync/runs:
    get:
      tags:
      - Admin
      summary: Recent player sync runs
      description: Outcome (counts) and the diff (players added/removed) of the most
        recent syncs.
      operationId: playerSyncRuns
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 10
      responses:
        "200":
          description: Runs returned
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SyncRunResponse"
components:
  schemas:
    DraftPick:
      type: object
      properties:
        playerId:
          type: integer
          format: int32
        teamId:
          type: string
          maxLength: 64
          minLength: 0
      required:
      - playerId
      - teamId
    DraftState:
      type: object
      properties:
        teams:
          type: array
          items:
            $ref: "#/components/schemas/DraftTeam"
          maxItems: 32
          minItems: 0
        order:
          type: array
          items:
            type: string
            maxLength: 64
            minLength: 0
          maxItems: 32
          minItems: 0
        picks:
          type: array
          items:
            $ref: "#/components/schemas/DraftPick"
        finishedAt:
          type: string
          format: date-time
      required:
      - order
      - picks
      - teams
    DraftTeam:
      type: object
      properties:
        id:
          type: string
          maxLength: 64
          minLength: 0
        name:
          type: string
          maxLength: 64
          minLength: 0
        mine:
          type: boolean
      required:
      - id
      - mine
      - name
    PlayerProjection:
      type: object
      properties:
        playerId:
          type: integer
          format: int32
        type:
          type: string
          enum:
          - skater
          - goalie
        stats:
          $ref: "#/components/schemas/PlayerStats"
      required:
      - playerId
      - stats
      - type
    PlayerStats:
      type: object
      properties:
        utility:
          type: object
          additionalProperties:
            type: number
            format: double
        scoring:
          type: object
          additionalProperties:
            type: number
            format: double
      required:
      - scoring
      - utility
    ProjectionData:
      type: object
      properties:
        settings:
          $ref: "#/components/schemas/ProjectionSettings"
        players:
          type: array
          items:
            $ref: "#/components/schemas/PlayerProjection"
        draft:
          $ref: "#/components/schemas/DraftState"
        tings:
          $ref: "#/components/schemas/ProjectionData"
          writeOnly: true
      required:
      - players
      - settings
    ProjectionSettings:
      type: object
      properties:
        scoringType:
          type: string
          enum:
          - points
          - category
        statWeights:
          type: object
          additionalProperties:
            type: number
            format: double
        activeScoringColumns:
          type: array
          items:
            type: string
        activeUtilityColumns:
          type: array
          items:
            type: string
        scaleSettings:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ScaleConfig"
        decimalSettings:
          type: object
          additionalProperties:
            type: integer
            format: int32
        useDefaultDecimals:
          type: boolean
        leagueSize:
          type: integer
          format: int32
          maximum: 30
          minimum: 2
        rosterSlots:
          $ref: "#/components/schemas/RosterSlots"
        minGoalieGames:
          type: integer
          format: int32
          maximum: 82
          minimum: 0
        yahooSync:
          $ref: "#/components/schemas/YahooSync"
      required:
      - activeScoringColumns
      - activeUtilityColumns
      - decimalSettings
      - scaleSettings
      - scoringType
      - statWeights
      - useDefaultDecimals
    RosterSlots:
      type: object
      properties:
        c:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        lw:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        rw:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        d:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        util:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        bn:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
        g:
          type: integer
          format: int32
          maximum: 50
          minimum: 0
      required:
      - bn
      - c
      - d
      - g
      - lw
      - rw
      - util
    ScaleConfig:
      type: object
      properties:
        scale:
          type: boolean
        scalableStats:
          type: array
          items:
            type: string
      required:
      - scalableStats
      - scale
    UpdateProjectionRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 100
          minLength: 0
        data:
          $ref: "#/components/schemas/ProjectionData"
      required:
      - data
      - name
    YahooSync:
      type: object
      properties:
        leagueName:
          type: string
          maxLength: 200
          minLength: 0
        leagueKey:
          type: string
          maxLength: 100
          minLength: 0
        syncedAt:
          type: string
          format: date-time
      required:
      - leagueKey
      - leagueName
      - syncedAt
    ProjectionResponse:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        season:
          type: string
          enum:
          - "20252026"
          - "20262027"
          - "20272028"
          - "20282029"
        data:
          $ref: "#/components/schemas/ProjectionData"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
      - createdAt
      - data
      - id
      - name
      - season
      - updatedAt
    EspnCredentialsRequest:
      type: object
      properties:
        espnS2:
          type: string
          description: The user's espn_s2 cookie value.
          maxLength: 1024
          minLength: 0
        swid:
          type: string
          description: The user's SWID cookie value (including the braces).
          maxLength: 128
          minLength: 0
      required:
      - espnS2
      - swid
    AuthorizeUrlResponse:
      type: object
      properties:
        authorizeUrl:
          type: string
      required:
      - authorizeUrl
    CreateProjectionRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 100
          minLength: 0
        data:
          $ref: "#/components/schemas/ProjectionData"
      required:
      - data
      - name
    PortalUrlResponse:
      type: object
      properties:
        portalUrl:
          type: string
          description: Provider-hosted customer-portal URL to redirect the browser
            to
      required:
      - portalUrl
    CheckoutUrlResponse:
      type: object
      properties:
        checkoutUrl:
          type: string
          description: Provider-hosted checkout URL to redirect the browser to
      required:
      - checkoutUrl
    VerifyEmailRequest:
      type: object
      properties:
        token:
          type: string
          maxLength: 128
          minLength: 0
      required:
      - token
    ResendVerificationRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          minLength: 0
      required:
      - email
    RegisterRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          minLength: 0
        password:
          type: string
          maxLength: 72
          minLength: 8
      required:
      - email
      - password
    AuthResponse:
      type: object
      properties:
        token:
          type: string
        expiresInSeconds:
          type: integer
          format: int64
        refreshToken:
          type: string
        refreshExpiresInSeconds:
          type: integer
          format: int64
        admin:
          type: boolean
        emailVerified:
          type: boolean
          description: Whether the account's email address has been verified
      required:
      - admin
      - emailVerified
      - expiresInSeconds
      - refreshExpiresInSeconds
      - refreshToken
      - token
    RefreshRequest:
      type: object
      properties:
        refreshToken:
          type: string
          maxLength: 4096
          minLength: 0
      required:
      - refreshToken
    ResetPasswordRequest:
      type: object
      properties:
        token:
          type: string
          maxLength: 128
          minLength: 0
        newPassword:
          type: string
          maxLength: 72
          minLength: 8
      required:
      - newPassword
      - token
    ForgotPasswordRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          minLength: 0
      required:
      - email
    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          minLength: 0
        password:
          type: string
          maxLength: 72
          minLength: 0
      required:
      - email
      - password
    GoogleLoginRequest:
      type: object
      properties:
        idToken:
          type: string
          description: Google ID token (JWT) issued by Google Identity Services
          maxLength: 4096
          minLength: 0
      required:
      - idToken
    GoogleCodeLoginRequest:
      type: object
      properties:
        code:
          type: string
          description: Authorization code returned by Google's OAuth redirect
          maxLength: 2048
          minLength: 0
        redirectUri:
          type: string
          description: Redirect URI used in the authorization request; must match
            one the BFF allows
          maxLength: 512
          minLength: 0
      required:
      - code
      - redirectUri
    FacebookLoginRequest:
      type: object
      properties:
        accessToken:
          type: string
          description: Facebook user access token from the Facebook Login SDK
          maxLength: 4096
          minLength: 0
      required:
      - accessToken
    SyncAcceptedResponse:
      type: object
      properties:
        status:
          type: string
      required:
      - status
    LeagueSummary:
      type: object
      properties:
        leagueKey:
          type: string
        name:
          type: string
        season:
          type: integer
          format: int32
        numTeams:
          type: integer
          format: int32
        scoringType:
          type: string
      required:
      - leagueKey
      - name
    LeaguesResponse:
      type: object
      properties:
        leagues:
          type: array
          items:
            $ref: "#/components/schemas/LeagueSummary"
      required:
      - leagues
    LeagueTeam:
      type: object
      properties:
        name:
          type: string
        mine:
          type: boolean
      required:
      - mine
      - name
    LeagueTeamsResponse:
      type: object
      properties:
        teams:
          type: array
          items:
            $ref: "#/components/schemas/LeagueTeam"
      required:
      - teams
    LeagueProjectionSettingsResponse:
      type: object
      properties:
        scoringType:
          type: string
          enum:
          - points
          - category
        activeScoringColumns:
          type: array
          items:
            type: string
        activeUtilityColumns:
          type: array
          items:
            type: string
        statWeights:
          type: object
          additionalProperties:
            type: number
            format: double
          description: Per-stat point weights for points leagues; null for category
            leagues.
        rosterSlots:
          $ref: "#/components/schemas/RosterSlots"
        leagueSize:
          type: integer
          format: int32
          description: League size from the Yahoo league's team count; null when unknown.
        unsupportedStats:
          type: array
          items:
            type: string
        unsupportedRosterCodes:
          type: array
          items:
            type: string
      required:
      - activeScoringColumns
      - activeUtilityColumns
      - rosterSlots
      - scoringType
      - unsupportedRosterCodes
      - unsupportedStats
    ConnectionResponse:
      type: object
      properties:
        connected:
          type: boolean
      required:
      - connected
    ServiceVersion:
      type: object
      properties:
        name:
          type: string
        up:
          type: boolean
        version:
          type: string
          description: Deployed version; absent when the service is unreachable
      required:
      - name
      - up
    VersionsResponse:
      type: object
      properties:
        services:
          type: array
          items:
            $ref: "#/components/schemas/ServiceVersion"
      required:
      - services
    ProjectionSummaryResponse:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        season:
          type: string
          enum:
          - "20252026"
          - "20262027"
          - "20272028"
          - "20282029"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        draftStatus:
          type: string
          enum:
          - none
          - in_progress
          - finished
      required:
      - createdAt
      - draftStatus
      - id
      - name
      - season
      - updatedAt
    PlayerSplitResponse:
      type: object
      description: A player's measured totals over a range of their team's games
      properties:
        playerId:
          type: integer
          format: int32
          description: The platform's player id
        name:
          type: string
        teamAbbrev:
          type: string
        type:
          type: string
          description: skater or goalie
        games:
          type: integer
          format: int32
          description: Games the player appeared in within the range
        firstTeamGame:
          type: integer
          format: int32
          description: First team game number covered
        lastTeamGame:
          type: integer
          format: int32
          description: Last team game number covered
        stats:
          type: object
          additionalProperties:
            type: number
            format: double
          description: Totals over the range
      required:
      - games
      - name
      - playerId
      - stats
      - type
    SeededProjectionResponse:
      type: object
      description: "Projection lines generated by the model, keyed by the platform's\
        \ player id"
      properties:
        players:
          type: array
          items:
            $ref: "#/components/schemas/PlayerProjection"
        season:
          type: integer
          format: int32
          description: Season the lines project
        modelVersion:
          type: string
          description: Model version behind them
        skaters:
          type: integer
          format: int32
        goalies:
          type: integer
          format: int32
        unmapped:
          type: integer
          format: int32
          description: Projected players with no counterpart on this platform. Mostly
            fringe and long-term absent players the platform doesn't carry.
        goaliesWithoutWorkload:
          type: integer
          format: int32
          description: "Goalies the model projects no starts for. Left out rather\
            \ than seeded with a zero save percentage, which would read as the worst\
            \ goalie in the league."
      required:
      - goalies
      - goaliesWithoutWorkload
      - modelVersion
      - players
      - season
      - skaters
      - unmapped
    SkaterResponse:
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string
        teamAbbrev:
          type: string
        headshot:
          type: string
        positions:
          type: array
          items:
            type: string
            enum:
            - LW
            - C
            - RW
            - D
          uniqueItems: true
        stats:
          $ref: "#/components/schemas/SkaterStats"
      required:
      - id
      - name
      - positions
      - stats
    SkaterScoringStats:
      type: object
      properties:
        goals:
          type: integer
          format: int32
        assists:
          type: integer
          format: int32
        points:
          type: integer
          format: int32
        plusMinus:
          type: integer
          format: int32
        pim:
          type: integer
          format: int32
        ppg:
          type: integer
          format: int32
        ppa:
          type: integer
          format: int32
        ppp:
          type: integer
          format: int32
        shg:
          type: integer
          format: int32
        sha:
          type: integer
          format: int32
        shp:
          type: integer
          format: int32
        gwg:
          type: integer
          format: int32
        sog:
          type: integer
          format: int32
        shPct:
          type: number
          format: double
        fw:
          type: integer
          format: int32
        fl:
          type: integer
          format: int32
        hits:
          type: integer
          format: int32
        blocks:
          type: integer
          format: int32
      required:
      - assists
      - blocks
      - fl
      - fw
      - goals
      - gwg
      - hits
      - pim
      - plusMinus
      - points
      - ppa
      - ppg
      - ppp
      - shPct
      - sha
      - shg
      - shp
      - sog
    SkaterStats:
      type: object
      properties:
        utility:
          $ref: "#/components/schemas/SkaterUtilityStats"
        scoring:
          $ref: "#/components/schemas/SkaterScoringStats"
      required:
      - scoring
      - utility
    SkaterUtilityStats:
      type: object
      properties:
        gp:
          type: integer
          format: int32
        toiPerGame:
          type: integer
          format: int32
      required:
      - gp
      - toiPerGame
    GoalieResponse:
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string
        teamAbbrev:
          type: string
        headshot:
          type: string
        stats:
          $ref: "#/components/schemas/GoalieStats"
      required:
      - id
      - name
      - stats
    GoalieScoringStats:
      type: object
      properties:
        gs:
          type: integer
          format: int32
        w:
          type: integer
          format: int32
        l:
          type: integer
          format: int32
        sho:
          type: integer
          format: int32
        sa:
          type: integer
          format: int32
        sv:
          type: integer
          format: int32
        ga:
          type: integer
          format: int32
        gaa:
          type: number
          format: double
        svPct:
          type: number
          format: double
      required:
      - ga
      - gaa
      - gs
      - l
      - sa
      - sho
      - sv
      - svPct
      - w
    GoalieStats:
      type: object
      properties:
        utility:
          $ref: "#/components/schemas/GoalieUtilityStats"
        scoring:
          $ref: "#/components/schemas/GoalieScoringStats"
      required:
      - scoring
      - utility
    GoalieUtilityStats:
      type: object
      properties:
        gp:
          type: integer
          format: int32
      required:
      - gp
    EspnLeagueTeam:
      type: object
      properties:
        name:
          type: string
        mine:
          type: boolean
          description: True for the authenticated user's own team (matched via their
            SWID; always false for public leagues read without cookies).
      required:
      - mine
      - name
    EspnLeagueTeamsResponse:
      type: object
      properties:
        teams:
          type: array
          items:
            $ref: "#/components/schemas/EspnLeagueTeam"
      required:
      - teams
    CredentialStatusResponse:
      type: object
      properties:
        hasCredentials:
          type: boolean
      required:
      - hasCredentials
    EntitlementsResponse:
      type: object
      properties:
        premium:
          type: boolean
          description: Whether the user currently has premium access
        status:
          type: string
          description: "Subscription status code, or \"none\" when there is no subscription"
        currentPeriodEnd:
          type: string
          format: date-time
          description: When the current paid period ends; null when not applicable
        cancelAtPeriodEnd:
          type: boolean
          description: Whether the subscription is set to cancel at period end
      required:
      - cancelAtPeriodEnd
      - premium
      - status
    SyncRunResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        status:
          type: string
        skaters:
          type: integer
          format: int32
        goalies:
          type: integer
          format: int32
        addedCount:
          type: integer
          format: int32
        removedCount:
          type: integer
          format: int32
        added:
          type: array
          items:
            type: string
        removed:
          type: array
          items:
            type: string
        error:
          type: string
      required:
      - added
      - addedCount
      - id
      - removed
      - removedCount
      - startedAt
      - status
  securitySchemes:
    bearerAuth:
      type: http
      name: bearerAuth
      scheme: bearer
      bearerFormat: JWT
